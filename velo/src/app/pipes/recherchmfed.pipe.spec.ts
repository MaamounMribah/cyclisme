import { RecherchmfedPipe } from './recherchmfed.pipe';

describe('RecherchmfedPipe', () => {
  it('create an instance', () => {
    const pipe = new RecherchmfedPipe();
    expect(pipe).toBeTruthy();
  });
});
