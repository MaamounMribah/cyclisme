import { RechercoureurPipe } from './rechercoureur.pipe';

describe('RechercoureurPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercoureurPipe();
    expect(pipe).toBeTruthy();
  });
});
